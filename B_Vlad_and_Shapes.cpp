

#include <iostream>
#include <bits/stdc++.h>
using namespace std;
 

long long main() {
    

   long long n;
   cin>>n;
   while(n--){
    long long size;
    cin>>size;
    long long prev = -1;
    for(long long i = 0;i<size;i++){
        long long sum = 0;
        long long long longprev = -1;
        for(long long j  0;j<size;j++){
            long long x;
            cin>x;
            if(x == 1 && (long longprwv == 1|| long longprev == -1))sum++;
            if(x == 1) long longprev = 1;
        }
        if(sum == prev)flag = 1;
        if(sum )

    }
   }

}


