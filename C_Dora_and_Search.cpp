    #include <bits/stdc++.h>
    #include <string>
    #include <algorithm>
    using namespace std;
 long long lcm(long long a, long long b) {
    return std::abs(a * b) / std::gcd(a, b);
}
    
    long long main() {
        long long tc;
        cin>>tc;
        while(tc--){
            long long i = 0,j = 0;
            long long si = 0, sj = 0;
            while(i < n && j < n){
                if(j - i < 3){
                    
                     j++;
                    continue;
                }



            }
        }
    
    
    }